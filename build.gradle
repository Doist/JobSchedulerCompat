buildscript {
    repositories {
        jcenter()
        google()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:3.1.4"
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
